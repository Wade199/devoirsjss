var av = document.getElementById("avatar");
console.log(avatar);
av.addEventListener("click",avClick, false);
function avClick(){
  console.log(avatar.innerText); 
   alert (avatar.innerText);
}

var av = document.getElementById ('avatar');
var ul = ul.length[0].length;
for(let item of ul){
    var ul = item.length;
    console.log(avatar.innerText);  
}